# 바른자세 — 실시간 자세 교정 앱

웹캠 기반 실시간 자세 분석 및 교정 알림 애플리케이션입니다.
MediaPipe Pose를 활용해 목 굴곡, 머리 앞돌출, 측방 기울기, 어깨 기울기를 측정하고
자체 개발 채점 기준인 **PSI(Posture Score Index)** 로 점수를 산출합니다.

---

## 주요 기능

- 실시간 웹캠 자세 분석 (30fps)
- PSI 18점 만점 채점 — 낮을수록 좋은 자세
- 5초 캘리브레이션으로 개인별 기준 설정
- 자세 등급에 따른 팝업 알림 (주의 / 경고 / 위험)
- 일별 캘린더 + 주간/월간 통계 차트
- 리포트 페이지: 기간별 평균 PSI, 시간대별 패턴, 등급 분포, 스트레칭 권장
- 히스토리 페이지: 날짜별 기록 조회
- 민감도 설정 (여유 / 균형 / 엄격)

---

## PSI 채점 기준

PSI는 4개 축을 독립 채점 후 가중 합산하는 방식입니다.

| 축 | 측정 항목 | 가중치 | 점수 범위 |
|---|---|---|---|
| 축1 | 목 전방 굴곡 (도) | x2 | 1~3 |
| 축2 | 머리 앞돌출 (cm) | x1 | 1~4 |
| 축3 | 측방 기울기 (도) | x1 | 1~4 |
| 축4 | 어깨 기울어짐 (도) | x1 | 1~4 |

**총점 = (축1 x 2) + 축2 + 축3 + 축4 — 범위: 5~18**

| PSI 점수 | 등급 | 의미 |
|---|---|---|
| 5 | 완벽 | 이상적인 자세 |
| 6 ~ 8 | 허용 | 양호한 자세 |
| 9 ~ 12 | 주의 | 자세 교정 권장 |
| 13 ~ 15 | 경고 | 즉시 교정 필요 |
| 16 ~ 18 | 위험 | 거북목 위험 |

---

## 측정 방식

- **목 굴곡**: MediaPipe world landmarks Y/Z 벡터의 atan2 각도 — 캘리브레이션 기준값 차이
- **머리 앞돌출**: world landmarks z축 코-어깨 거리 — 캘리브레이션 기준값 차이 (m 단위)
- **측방 기울기**: 눈~눈 선의 atan2 각도 (수평 대비 기울어진 각도)
- **어깨 기울기**: 양쪽 어깨 y좌표 차이의 atan2 각도
- 모든 측정값은 deque(maxlen=20) 이동 평균으로 스무딩

---

## 설치 및 실행

**요구사항**: Python 3.10, 웹캠

```bash
pip install -r requirements.txt
py posture.py
```

**requirements.txt**
```
opencv-python>=4.13.0
mediapipe>=0.10.5
numpy>=2.1.3
Pillow>=11.1.0
matplotlib>=3.10.1
plyer>=2.1.0
```

---

## 사용법

1. 앱 실행 후 웹캠 앞에 바르게 앉습니다.
2. 대시보드에서 **모니터링 시작** 버튼을 누릅니다.
3. **캘리브레이션** (5초): 현재 자세를 기준값으로 설정합니다.
4. 캘리브레이션 완료 후 카메라 창이 자동으로 숨겨지고 백그라운드 측정이 시작됩니다.
5. 자세가 나빠지면 화면에 경고 배너가 표시됩니다.
6. 카메라 창의 **다시 캘리브레이션** 버튼으로 기준 자세를 재설정할 수 있습니다.

---

## 데이터 저장 위치

측정 데이터는 JSON 파일로 자동 저장됩니다.

- Windows: `C:\Users\[사용자명]\.posture_app\posture_data.json`
- Mac / Linux: `/home/[사용자명]/.posture_app/posture_data.json`

앱을 처음 실행하면 자동으로 파일과 폴더가 생성됩니다.
다른 기기에서 데이터를 가져오려면 해당 경로에 JSON 파일을 복사하면 됩니다.

---

## 화면 구성

| 페이지 | 내용 |
|---|---|
| 대시보드 | 오늘 요약, 주간 PSI 차트, 캘린더, 모니터링 시작/종료 |
| 실시간 모니터링 | PSI 점수 링, 4축 수치, 세션 통계 |
| 리포트 | 기간별 통계, 시간대 패턴, 등급 분포, 스트레칭 권장 |
| 히스토리 | 날짜별 기록 목록 |
| 환경 설정 | 민감도, 알림 간격 설정 |

---

## 민감도 설정

| 설정 | 측방 기울기 임계값 |
|---|---|
| 여유 | 15도 이상 |
| 균형 (기본값) | 10도 이상 |
| 엄격 | 7도 이상 |

---

## 기술 스택

- Python 3.10
- MediaPipe Pose
- OpenCV
- tkinter
- Pillow
